export enum AccountState {
    Unknown = "Unknown",
    NoWeb3 = "Web3 is not detected in the browser",
    NoEthereum = "Ethereum is not detected in the browser",
    NoUnlocked = "Account is locked",
    Ok = "Ok"
}
